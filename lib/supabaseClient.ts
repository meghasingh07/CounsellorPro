/**
 * Named export matching the requested project layout (`lib/supabaseClient`).
 * Re-exports the browser client factory used across Client Components.
 */
export { createBrowserSupabaseClient as createSupabaseClient } from "./supabase/client";
